name: phone_shop_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  go_router: ^17.2.3
  shared_preferences: ^2.5.5
  cached_network_image: ^3.4.1
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  url_launcher: ^6.3.2
  shimmer: ^3.0.0
  confetti: ^0.8.0
  http: ^1.6.0
  flutter_svg: ^2.0.9  # ADD THIS

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/brands/   # ADD THIS
    - assets/icons/
    - assets/mock/